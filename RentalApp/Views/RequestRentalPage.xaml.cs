/*using RentalApp.ViewModels;

namespace RentalApp.Views;

public partial class RequestRentalPage : ContentPage
{
    // Shell will use this one
    public RequestRentalPage()
    {
        InitializeComponent();
        BindingContext = App.Services.GetRequiredService<RequestRentalViewModel>();
    }

    // DI constructor (optional but recommended)
    public RequestRentalPage(RequestRentalViewModel vm)
    {
        InitializeComponent();
        BindingContext = vm;
    }
}*/
